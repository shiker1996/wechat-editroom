specification {
  element person    { style { shape person } }
  element system    { style { shape rectangle } }
  element container { style { shape rectangle } }
  element component { style { shape component } }
  element storage   { style { shape storage } }

  tag core
  tag feature
  tag harness
  tag external

  relationship async { color amber; line dotted }
  relationship sync  { color primary; line solid }
  relationship data  { color green; line solid }
}