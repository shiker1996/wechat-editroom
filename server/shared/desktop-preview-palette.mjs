// Standalone iframe documents cannot inherit the workbench CSS variables.
// Keep their small preview shell on the same neutral desktop palette.
export const DESKTOP_PREVIEW_COLORS = Object.freeze({
  paper: '#f3f4f6',
  paperDeep: '#e9edf1',
  ink: '#30343b',
  line: 'rgba(48,52,59,.14)',
});
